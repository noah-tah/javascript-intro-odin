function capitalize(word) {
    let capitalizedWord = word.toUpperCase();
    return capitalizedWord;
}

let capitalizedWordToBePrinted = capitalize("word");

console.log("The capitalized word is: ", capitalizedWordToBePrinted);